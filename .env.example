# Homebox MCP Server Configuration

# LAN URL for your Homebox instance (required)
HOMEBOX_LOCAL_URL=http://10.0.0.4:7745

# (Optional) WAN/Public URL for external access
# When provided, WAN URL will be used for web links by default
# Leave empty if you only need LAN access
HOMEBOX_WAN_URL=

# (Optional) Use LAN for API calls
# Default: true (recommended for performance)
# Set to "false" to use WAN for API calls (slower)
USE_LAN_API=true

# (Optional) Use LAN for display links
# Default: false (uses WAN when available)
# Set to "true" to force LAN URLs in web links even when WAN is configured
LAN_LINKS=false

# URL Selection Logic:
#
# For API calls:
#   - USE_LAN_API=true (default) → Use LAN for API (faster)
#   - USE_LAN_API=false → Use WAN for API
#
# For web links:
#   - LAN_LINKS=true → Always use LAN URLs
#   - LAN_LINKS=false (default) → Use WAN if available, otherwise LAN

# Your Homebox credentials (required)
HOMEBOX_USERNAME=your_email@example.com
HOMEBOX_PASSWORD=your_password_here
